# Factory Profiles

Reference `.svcp` profile files (XML ValueTree format). Runtime factory templates are built in code; export a user profile here to share edits.

Import via the plugin UI: **Import** button accepts `.svcp` and `.xml`.
