# Smoke run — 2026-06-09 (v0.6.8 QA iteration)

## Automated (passed)

| Gate | Command | Result |
|------|---------|--------|
| Build | `cmake --build build` | OK |
| Engine tests | `ctest --test-dir build` | 15/15 pass |
| pluginval s5 | `./scripts/validate-plugins-local.sh` | VST3 + AU pass (CLAP skipped on macOS per PLUGINVAL.md) |
| QA script | `./scripts/qa-iterate.sh --round=1` | OK |

**Standalone binary:** `build/SuperVelocityCurve_artefacts/RelWithDebInfo/Standalone/Super VelocityCurve.app`  
**Timestamp:** 2026-06-09 02:01:29

## Bugs fixed this pass

1. Inspector floor/ceiling merge (live slider edits)
2. Profile switch dirty warning + discard reload
3. Save/export/session duplicate MIDI validation
4. Concertina section resize (bottom grip)
5. Calibration/histogram layout clipping

## Manual — still required (P1)

Walk [DAW_TEST_MATRIX.md](DAW_TEST_MATRIX.md):

- [ ] Ableton Live — Instrument VST3 on audio track
- [ ] Logic Pro — MIDI FX AU on software instrument track
- [ ] Min window size curve visible
- [ ] Pad switch curve persistence (hands-on MIDI)

Record host version and any failures in a new dated `SMOKE_RUN_*.md` when done.
